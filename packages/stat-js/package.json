{
  "name": "jser-stat",
  "version": "2.0.0",
  "description": "JSer.info stat library",
  "main": "lib/index.js",
  "browser": "lib/browser-index.js",
  "files": [
    "data/*.js",
    "lib",
    "src"
  ],
  "scripts": {
    "install": "node data/install-hook.js",
    "prepublish": "npm run --if-present build",
    "build": "babel src --out-dir lib --source-maps",
    "watch": "babel src --out-dir lib --watch --source-maps",
    "doc": "esdoc -c esdoc.json",
    "deploy": "npm run doc && gh-pages -d esdoc",
    "test": "mocha test && (cd example && npm test)"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jser/stat-js.git"
  },
  "keywords": [
    "jser"
  ],
  "author": "azu",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/jser/stat-js/issues"
  },
  "homepage": "https://github.com/jser/stat-js",
  "devDependencies": {
    "babel": "^5.5.8",
    "esdoc": "^0.2.1",
    "espower-babel": "^3.2.0",
    "gh-pages": "^0.3.1",
    "mocha": "^2.2.5",
    "power-assert": "^0.11.0"
  },
  "dependencies": {
    "array.prototype.find": "^1.0.0",
    "binarysearch": "^0.2.4",
    "natural": "^0.2.1",
    "request": "^2.57.0"
  }
}
